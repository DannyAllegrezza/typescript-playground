{
  "name": "pomodoro-timer",
  "version": "1.0.0",
  "description": "Pomodoro Timer.. in TypeScript!",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "nodemon",
    "start": "npm run build && node build/index.js"
  },
  "author": "danny allegrezza",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.1",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  }
}
